import { VStack } from "@chakra-ui/react";
import BlogCard from "./BlogCard";

const BlogContainer = () => {
  return (
    <VStack>
      <BlogCard />
    </VStack>
  );
};

export default BlogContainer;
